// Node box dimensions, shared by the renderer and the layout worker. Kept free
// of any reactflow/DOM import so the Web Worker bundle stays lean.
export const NODE_W = 188;
export const NODE_H = 56;
