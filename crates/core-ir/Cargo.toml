[package]
name = "core-ir"
version = "0.1.0"
edition = "2021"
description = "Phase-0 IR data model: language-neutral code graph (single source of truth)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
